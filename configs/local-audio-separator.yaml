pipeline:
  instrument: bass

normalize:
  sample_rate: 44100
  channels: 2
  codec: pcm_s16le

separation:
  backend: audio_separator
  requested_stems:
    - bass
    - guitar
  model_filename: htdemucs_6s.yaml
  model_file_dir: data/models/audio-separator